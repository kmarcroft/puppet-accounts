---
fixtures:
  forge_modules:
    stdlib:
      repo: "puppetlabs/stdlib"
      ref: "9.7.0"
    augeas_core:
      repo: "puppetlabs/augeas_core"
      ref: "1.5.0"
    sshkeys_core:
      repo: "puppetlabs/sshkeys_core"
      ref: "2.4.0"
  symlinks:
    accounts: "#{source_dir}"
